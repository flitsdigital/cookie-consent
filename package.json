{
  "name": "@flitsdigital/cookie-consent",
  "version": "1.0.0",
  "description": "Herbruikbare cookie consent (Consent Mode v2 + GTM) voor Webflow-sites van Flits Digital",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "pretest": "npm run build",
    "test": "playwright test",
    "preversion": "npm test",
    "version": "npm run build && node changelog.js && git add CHANGELOG.md dist",
    "release": "npm version"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "esbuild": "^0.25.0"
  }
}
